/*const app = require('./app.js');

/app.listen(app.get('port'), () => {
    console.log("Servidor escuchando en el puerto", app.get("port"));
}); 

const express = require('express');
const app = express();

app.get('/productos',(req, res) => {
    const index('Hola Prueba - Visualizar cambio'):
     
}) */

